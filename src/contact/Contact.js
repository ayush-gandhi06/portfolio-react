// ============================================================
//  Contact.js — v1  (thin wrapper)
// ============================================================
import React, { Component } from "react";
import ContactMenu from "./ContactMenu";

export default class Contact extends Component {
  render() {
    return <ContactMenu />;
  }
}